#include <stdio.h>
int main() {
    int a[5]={1,2,3,4,5};
    int*p;
    p=&a[0];
    p=p+2;
    printf("p is %d \n",*p);
   p= p+1;
    printf("p is %u \n",p);
    *p=2;
    printf("p is %d",*p);
    return 0;
}

#include <stdio.h>
int main() {
     int d;
     int a[5]={0,1,-1,10,11};
     int*p,*q;
     p=&a[0];
     q=&a[4];
     
     d=p-q;
     *q=25;
     d=q-p;
     *q=27;
     q=q-3;
     p=p+3;
     d=p-q;
     printf("p is %d,q is %d,d is %d \n",*p,*q,d);
     return 0;
     
}
 

#include <stdio.h>
int main() {
 int a;
 float b;
 char c;
 
 int *ptra;
 float *ptrb;
 char *ptrc;
 printf("variable size\n int:%zu \n",sizeof(a));
 printf("float:%zu \n",sizeof(b));
 printf("char:%zu \n",sizeof(c));
 
 printf("variable size\n int:%zu \n",sizeof(ptra));
 printf("float:%zu \n",sizeof(ptrb));
 printf("char:%zu \n",sizeof(ptrc));


     return 0;
     
}


#include <stdio.h>
int main() {
int *ptr=NULL;
if(ptr==NULL)
{printf("the pointer is null");
    
}
else
{printf("value is %d",*ptr);
}
return 0;
}