#include <stdio.h>
#include <string.h>
int main() {
    int i;
    char students[3][30];
    printf("enter the name of students\n");
    for(i=0;i<3;i++)
    {scanf("%s",&students[i]);
}
for(i=0;i<3;i++)
{printf("length of %s is %d\n",students[i],strlen(students[i]));
}
return 0;
}

FXNSSSSSS
#include <stdio.h>
float sum(float a,float b,float c);
int main() {
    float a,b,c;
    printf("enter a,b");
    scanf("%f %f",&a,&b);
    c=sum(a,b,c);
    printf("sum is %d",c);
    return 0;
}
float sum(float a,float b,float c) {
    c=a+b;
    return c;
}

#include <stdio.h>
int max(int a,int b,int c);
int main() {
    int a,b,c;
    printf("enterr the value of a,b,c");
    scanf("%d %d %d",&a,&b,&c);
    max(a,b,c);
    return 0;
}
int max(int a,int b,int c) {
    if(a>b && a>c) {
        printf("a is the greatest");
    }
    else  {
        if (b>c) {
            printf("a is the greatest");
        }
        else { 
            printf("c is the greatest");
        }
    }
}


#include <stdio.h>
int swap(int *a,int *b);
int main() {
    int a=3,b=5;
    swap(&a,&b);
    return 0;
}
int swap(int *a,int *b) {
    int temp=*a;
    *a=*b;
    *b=temp;
    printf("a is %d\n b is %d",*a,*b);
}


#include <stdio.h>
int sum_of_digits(int i);
int main() {
    int i=67,sum;
    sum=sum_of_digits(i);
    printf("sum is %d",sum);
    return 0;
}
int sum_of_digits(int i) {
    int sum=0,digits;
    while(i>0)
   { digits=i%10;
    sum=sum+digits;
    i=i/10;
    return sum;}
    
    
}int main() {
    struct student s;
    input student(&s);
    display_student(s);
    return 0;
}


#include <stdio.h>
struct student {
int id;
float marks;
};
void input_student(struct student) {
    printf("enter the value of student info");
    scanf("%d \n %f",&s->id,&s->marks);
}
void dislay_student(struct student) {
    printf("student info \n");
    printf("id %d \n",s.id);
    printf("id %f",s.marks);
}


#include <stdio.h>
#include <string.h>
struct my_struct {
    int i;
    char c[20];
    float f;
};
union my_union {
    int i;
    float f;
    char c[20];
};

int main() {
struct my_struct s={1,"jenny",69.6};
printf("\n === structure === \n");
printf("i of s is %d \n",s.i);
printf("c of s is %s \n",s.c);
printf("f of s is %f \n",s.f);
union my_union u;
printf("\n === union === \n");
u.i=181;
printf("i of u is %d \n",u.i);
u.f=181.60;
printf("f of u is %f \n",u.f);
strcpy(u.c, "jiya");
printf("c of u is %s \n",u.c);
return 0;
}

